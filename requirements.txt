# Big Finish Caption Sync - Python Dependencies

# Web server
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# HTTP requests (for OpenAI API)
requests>=2.31.0

# Optional: Local Whisper (install separately if needed)
# pip install openai-whisper
